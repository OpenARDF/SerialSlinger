# This file is normally generated by updateDaemonJvm.
# Keep the daemon on Java 17 so local desktop packaging uses the registered
# system JDK instead of a Gradle-downloaded Java 21 toolchain.
toolchainVersion=17
